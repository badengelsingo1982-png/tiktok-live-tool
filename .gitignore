node_modules/
auth.json
*.log
npm-debug.log*
.DS_Store
*.zip

# 実行時に生成/アップロードされるデータ（サーバーごとに固有。Git管理しない）
soundboard.json
gift-catalog.json
sounds/*.mp3
sounds/lib/
# public/icons/ は Git 管理する。自作アイコンをVPSへ配るため。
# server.js の ensureIcons() はファイルが無い時だけ生成するので上書きされない

# マルチユーザー版の実行時データ
users.json
session-secret
data/
